# CodersAlpha Manage Project Setup Guide (Hindi & English)

यह गाइड आपको CodersAlpha Manage प्रोजेक्ट (Laravel Backend + React Native Frontend) को आसानी से सेटअप और रन करने में मदद करेगी।
This guide helps you setup and run the CodersAlpha Manage project (Laravel Backend + React Native Frontend) easily.

---

## 🚀 Quick Start (तुरंत शुरू करें)

हमने आपके लिए एक ऑटोमेटेड स्क्रिप्ट तैयार की है।
We have created an automated script for you.

1. **टर्मिनल खोलें (Open Terminal)**
2. **प्रोजेक्ट फोल्डर में जाएं (Go to project folder)**
3. **कमांड चलाएं (Run command):**
   ```bash
   ./start-project.sh
   ```
   (अगर परमिशन एरर आए तो पहले `chmod +x start-project.sh` चलाएं)

---

## 🛠 Manual Setup (मैनुअल सेटअप)

### 1. Backend (InstituteProApi)

**Requirements:** PHP, Composer, MySQL

1. **फोल्डर में जाएं:**
   ```bash
   cd InstituteProApi
   ```

2. **डिपेंडेंसी इंस्टॉल करें:**
   ```bash
   composer install
   ```

3. **Environment फाइल सेटअप करें:**
   - `.env.example` को `.env` में कॉपी करें (अगर नहीं है)।
   - डेटाबेस सेटिंग्स सही करें (`DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD`)।
   - सुनिश्चित करें `APP_URL=http://192.168.1.71:8000` (या आपका कंप्यूटर IP)।

4. **डेटाबेस माइग्रेट करें:**
   ```bash
   php artisan migrate
   ```

5. **सर्वर स्टार्ट करें:**
   ```bash
   composer start
   ```
   *यह कमांड ऑटोमैटिकली आपके नेटवर्क IP (0.0.0.0) पर सर्वर चलाएगा।*

### 2. Frontend (InstituteProApp)

**Requirements:** Node.js, npm/yarn, Expo Go App (Mobile)

1. **फोल्डर में जाएं:**
   ```bash
   cd InstituteProApp
   ```

2. **डिपेंडेंसी इंस्टॉल करें:**
   ```bash
   npm install
   ```

3. **API URL कॉन्फ़िगरेशन (अगर जरूरत हो):**
   - `src/services/api.ts` फाइल चेक करें।
   - यह ऑटोमैटिकली आपके कंप्यूटर का IP डिटेक्ट करने की कोशिश करता है, लेकिन अगर नेटवर्क एरर आए, तो `getBaseUrl` में IP मैन्युअली सेट करें।

4. **ऐप स्टार्ट करें:**
   ```bash
   npm start
   ```
   - फिर "s" दबाएं (Expo Go के लिए) या "a" (Android Emulator के लिए) या "i" (iOS Simulator के लिए)।

---

## 🔧 Troubleshooting (समस्या निवारण)

### 1. "Network Error" / API Connect नहीं हो रहा
- **Backend Check:** ब्राउज़र में `http://localhost:8000/api/auth/login` खोलें। अगर `401 Unauthorized` या JSON रिस्पॉन्स मिले, तो बैकएंड सही है।
- **IP Check:** सुनिश्चित करें कि मोबाइल और कंप्यूटर **एक ही Wi-Fi** पर हैं।
- **Firewall:** कभी-कभी Firewall पोर्ट 8000 को ब्लॉक करता है। इसे चेक करें।

### 2. "Address already in use" (Port 8000)
- अगर सर्वर पहले से चल रहा है, तो उसे बंद करने के लिए यह कमांड चलाएं:
  ```bash
  lsof -ti :8000 | xargs kill -9
  ```

### 3. "Route [login] not defined"
- यह एरर फिक्स कर दिया गया है। `routes/api.php` में अब लॉगिन रूट सही तरीके से डिफाइंड है।

---

## ✅ API Testing

आप इन एंडपॉइंट्स को टेस्ट कर सकते हैं:
- **Login:** `POST /api/auth/login`
- **Register:** `POST /api/auth/register`
- **Employees:** `GET /api/employees` (Requires Auth Token)

---

## 📱 Features
- **Dashboard:** Operational metrics & financials.
- **Tasks:** Kanban-style task management.
- **Finance:** Income/Expense tracking.
- **Attendance:** QR/Manual attendance.
